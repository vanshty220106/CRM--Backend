# This directory is used for local JSON file storage when MongoDB Atlas is unavailable.
# JSON files in this directory are git-ignored (they contain runtime data).
# The directory itself is tracked so it exists in fresh clones.
